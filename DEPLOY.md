# 🏦 儿童时间银行 — 免费部署指南

技术栈：Vite + React 前端，Cloudflare Pages Functions 后端，Cloudflare D1（SQLite）数据库。
全部托管在 Cloudflare 免费套餐上：**不备案、不买服务器、不用数据库服务器**，家庭使用免费额度绰绰有余。

> 为什么选 Cloudflare：`pages.dev` 域名在国内一般可直接访问（比 vercel.app 稳），数据库和托管在同一个免费账号下，步骤最少。

---

## 一、准备工作（都是免费的）

1. **GitHub 账号**：https://github.com/signup
2. **Cloudflare 账号**：https://dash.cloudflare.com/sign-up

---

## 二、把代码传到 GitHub

在本项目目录执行：

```sh
git init
git add .
git commit -m "儿童时间银行 MVP"
git branch -M main
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

---

## 三、创建并绑定数据库（D1）

1. 打开 Cloudflare 控制台 → 左侧 **Workers & Pages** → **D1**（数据库）
2. 点 **创建数据库**：
   - 名称填 `kid-time-bank-db`
   - 位置随便选（离你近一点的区域会更快，可选亚洲）
3. 创建完成，**先别关页面**，把数据库 ID 复制出来备用

> 不需要手动建表！应用第一次被访问时会自动建表。

---

## 四、部署网站（Pages）

1. Cloudflare 控制台 → **Workers & Pages** → **创建** → **Pages** → **连接到 Git**
2. 授权 GitHub，选刚才的仓库，点 **开始设置**
3. 构建设置里填：
   - 框架预设：**Vite**（或直接手动填下面两项）
   - 构建命令：`npm run build`
   - 构建输出目录：`dist`
4. 点 **保存并部署**，等 1~2 分钟
5. 部署成功后你会得到一个网址：`https://你的项目名.pages.dev` —— 现在网站已经能访问了

---

## 五、把数据库绑定给网站（关键一步）

1. 进入刚才创建的 Pages 项目 → 右上角 **设置** → **函数** → **D1 数据库绑定**
2. 点 **添加绑定**：
   - 变量名称填：`DB`（必须大写，代码里用的是这个名字）
   - 数据库：选 `kid-time-bank-db`
3. 保存后，回到 **部署** 标签页，点你那条部署右侧的 **…** → **重试部署**，让新绑定生效
4. 再访问 `https://你的项目名.pages.dev`：
   - 打开「我是家长」→「创建家庭」，注册成功就说明数据库连接正常 ✅

---

## 六、上线后怎么用

- **家长**：注册家庭 → 「设置」里添加小朋友（设置 4~6 位数字密码）→ 把页面顶部的**家庭码**告诉孩子
- **小朋友**：打开网站 → 我是小朋友 → 输入家庭码 → 点头像 → 输密码进入
- 家长在「任务」发任务、「奖励」上架奖品、「设置」调零花钱兑换比例和定期利率

### 常见检查清单

| 现象 | 处理 |
|---|---|
| 注册时报错 | 检查第 5 步 D1 绑定是否完成、变量名是否叫 `DB` |
| 页面打不开 | 等部署完成；或点「重试部署」 |
| 想要自己的域名 | Pages → 项目 → **自定义域**，按提示在域名商处加一条 CNAME 解析即可（Cloudflare 免费 DNS） |

---

## 七、免费额度说明（家庭使用完全够）

- **D1 数据库**：5 GB 存储，每天 500 万次行读取、10 万次写入
- **Pages / Functions**：每天 10 万次请求
- 这些对「一个家庭每天几十次打卡」来说，用几年都到不了上限

---

## 八、本地开发（可选）

```sh
npm install
npm run build        # 先构建前端
npx wrangler pages dev   # 启动本地环境（含本地数据库模拟），默认 http://localhost:8788
```

---

## 项目结构速览

```
functions/api/          后端接口（Cloudflare Pages Functions）
  _lib.ts               共享逻辑：建表引导、登录、账本、利息结算
  register/login/...    家长注册登录、孩子登录、任务、银行、兑换等
src/pages/child/        孩子端界面（大厅/任务/银行/商店/我的）
src/pages/parent/       家长端界面（概览/审批/任务/奖励/设置）
```

## 规则默认值

| 项目 | 默认值 | 家长可否改 |
|---|---|---|
| 零花钱兑换 | 10 时币 = 1 元 | ✅ 设置页 |
| 7 天定期利率 | 1% | ✅ 设置页 |
| 14 天定期利率 | 2% | ✅ 设置页 |
| 30 天定期利率 | 4% | ✅ 设置页 |
